{
  "name": "conorcussell.co.uk",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "eslint src && preact test",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build && node build.js",
    "now-build": "preact build && node build.js",
    "now-start": "serve ./build",
    "serve": "preact build && preact serve",
    "dev": "preact watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "eslint": "^4.5.0",
    "eslint-config-synacor": "^1.1.0",
    "glob": "^7.1.2",
    "if-env": "^1.0.0",
    "preact-cli": "^1.4.1"
  },
  "dependencies": {
    "preact": "^8.2.4",
    "preact-compat": "^3.17.0",
    "preact-markup": "^1.6.0",
    "preact-router": "^2.5.7",
    "serve": "^6.1.0",
    "tachyons": "^4.8.1",
    "uncss": "^0.15.0"
  }
}
